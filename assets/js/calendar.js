window.Orbit = window.Orbit || {};
(function (Orbit) { Orbit.calendar = { month: new Date().getMonth(), year: new Date().getFullYear(), render() { const first = new Date(this.year, this.month, 1); const days = new Date(this.year, this.month + 1, 0).getDate(); const offset = first.getDay(); const monthName = first.toLocaleDateString(undefined, { month: 'long', year: 'numeric' }); const tasks = Orbit.storage.getTasks(); let cells = ''; for (let i = 0; i < offset; i += 1) cells += '<div class="calendar-cell muted-day"></div>'; for (let day = 1; day <= days; day += 1) { const date = `${this.year}-${String(this.month + 1).padStart(2,'0')}-${String(day).padStart(2,'0')}`; const dayTasks = tasks.filter(task => task.dueDate === date); const todayClass = date === new Date().toISOString().slice(0,10) ? 'today' : ''; cells += `<div class="calendar-cell ${todayClass}"><div class="calendar-number">${day}</div>${dayTasks.slice(0,3).map(task => `<div class="calendar-task" title="${Orbit.escape(task.title)}">${Orbit.escape(task.title)}</div>`).join('')}</div>`; } return `<div class="page-heading"><div><div class="eyebrow">Planning view</div><h1>Calendar</h1><p class="muted">See your commitments in context.</p></div></div><section class="calendar-shell card"><div class="calendar-controls"><button class="icon-btn" data-calendar="prev">←</button><h2>${monthName}</h2><button class="icon-btn" data-calendar="next">→</button></div><div class="calendar-grid">${['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].map(day => `<div class="calendar-weekday">${day}</div>`).join('')}${cells}</div></section>`; } }; })(window.Orbit);
